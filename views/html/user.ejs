<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Page</title>
  </head>
  <body>
    <h1>To Do List</h1>
    <h2>Hi, <%=username%>. </h2>
    <p><strong>Here's your to do list:</strong></p>
    <ol>
        <% if (data && data.length) { %>
          <% data.forEach(item => { %>
            <li><%= item.list %></li>
          <% }); %>
        <% } %>
      </ol>      
    <form action="/api/create/<%= id %>" method="POST">
        <label for="list">List:</label>
        <input type="text" id="list" name="list" placeholder="Enter your activities" pattern="[A-Za-z\s]+" title="Please enter a valid text string" required>
        <br>
        <button type="submit">Add</button>
    </form>
    <form action="/api/delete/<%= id %>" method="POST">
      <label for="num">Number:</label>
      <input type="number" id="num" name="num" placeholder="Enter number" min="1" required>
      <br>
      <button type="submit">Delete</button>
  </form>
  <form action="/api/home" method="GET">
    <label for="logout"></label>
    <br>
    <button type="logout">Logout</button>
</form>
  </body>
</html>
